@* NullifyItem - A redirect to the MenuEditor page.
                 This ensures that the final URL is for
                 the standard editor page. *@

@{ Response.Redirect("~/MenuEditor/MenuEditor");}